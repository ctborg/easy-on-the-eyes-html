function playlist(seed){const tracks=[];for(let i=0;i<seed.length;i++){if(seed[i].score>0.5){tracks.push({name:seed[i].name,artist:seed[i].artist,tags:seed[i].tags.map(t=>t.toLowerCase())});}}return tracks.sort((a,b)=>a.name.localeCompare(b.name)).map(track=>`${track.artist} - ${track.name}`).join('\n');}console.log(playlist([{name:'Song',artist:'Someone',score:.9,tags:['Mood','Late']},{name:'Skip',artist:'Nobody',score:.1,tags:[]}]));
